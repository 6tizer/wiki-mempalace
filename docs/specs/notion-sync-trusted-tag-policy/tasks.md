# Tasks: Notion Sync Trusted Tag Policy

- [x] Document Notion AI auto-fill as a trusted upstream tag classifier.
- [x] Add CLI enum values for `trusted-source`, `strict`, and `bootstrap`.
- [x] Make `trusted-source` the default for `notion-sync`.
- [x] Apply trusted-source policy to in-memory schema only.
- [x] Add tests for strict unchanged behavior and trusted-source source-tag acceptance.
- [x] Run focused tests plus fmt/clippy.
